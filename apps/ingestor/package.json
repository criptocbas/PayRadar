{
  "name": "@payradar/ingestor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/cli.ts",
    "sync": "tsx src/cli.ts sync-catalog",
    "probe": "tsx src/cli.ts run-probes",
    "score": "tsx src/cli.ts run-scoring",
    "all": "tsx src/cli.ts all",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@payradar/schema": "workspace:*",
    "@payradar/scoring-engine": "workspace:*",
    "@supabase/supabase-js": "^2.45.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
