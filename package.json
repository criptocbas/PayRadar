{
  "name": "payradar",
  "private": true,
  "version": "0.1.0",
  "description": "Trust + Discovery Intelligence Layer for the pay.sh catalog",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "db:migrate": "supabase db push",
    "db:reset": "supabase db reset"
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.6.2",
    "@types/node": "^22.7.4",
    "tsx": "^4.19.1"
  }
}
