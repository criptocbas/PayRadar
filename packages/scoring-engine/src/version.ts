// Bumped any time aggregation logic or default weights change.
// Dimension scorers carry their own per-dimension VERSION constants
// so replay can find the exact code path that produced a historical score.
export const ENGINE_VERSION = '0.1.0';
